package info.nightscout.androidaps.plugins.pump.carelevo.data.mapper

import info.nightscout.androidaps.plugins.pump.carelevo.data.model.entities.CarelevoPatchInfoEntity
import info.nightscout.androidaps.plugins.pump.carelevo.domain.model.patch.CarelevoPatchInfoDomainModel
import org.joda.time.DateTime

internal fun CarelevoPatchInfoEntity.transformToCarelevoPatchInfoDomainModel() = CarelevoPatchInfoDomainModel(
    address = address,
    createdAt = DateTime.parse(createdAt),
    updatedAt = DateTime.parse(updatedAt),
    manufactureNumber = manufactureNumber,
    firmwareVersion = firmwareVersion,
    bootDateTime = bootDateTime,
    bootDateTimeUtcMillis = bootDateTimeUtcMillis,
    modelName = modelName,
    insulinAmount = insulinAmount,
    insulinRemain = insulinRemain,
    thresholdInsulinRemain = thresholdInsulinRemain,
    thresholdExpiry = thresholdExpiry,
    thresholdMaxBasalSpeed = thresholdMaxBasalSpeed,
    thresholdMaxBolusDose = thresholdMaxBolusDose,
    checkSafety = checkSafety,
    checkNeedle = checkNeedle,
    needleFailedCount = needleFailedCount,
    isConnected = isConnected,
    needDiscard = needDiscard,
    isDiscard = isDiscard,
    isExtended = isExtended,
    isValid = isValid,
    isStopped = isStopped,
    stopMinutes = stopMinutes,
    stopMode = stopMode,
    isForceStopped = isForceStopped,
    runningMinutes = runningMinutes,
    infusedTotalBasalAmount = infusedTotalBasalAmount,
    infusedTotalBolusAmount = infusedTotalBolusAmount,
    pumpState = pumpState,
    mode = mode,
    bolusActionSeq = bolusActionSeq
)

internal fun CarelevoPatchInfoDomainModel.transformToCarelevoPatchInfoEntity() = CarelevoPatchInfoEntity(
    address = address,
    createdAt = createdAt.toString(),
    updatedAt = updatedAt.toString(),
    manufactureNumber = manufactureNumber,
    firmwareVersion = firmwareVersion,
    bootDateTime = bootDateTime,
    bootDateTimeUtcMillis = bootDateTimeUtcMillis,
    modelName = modelName,
    insulinAmount = insulinAmount,
    insulinRemain = insulinRemain,
    thresholdInsulinRemain = thresholdInsulinRemain,
    thresholdExpiry = thresholdExpiry,
    thresholdMaxBasalSpeed = thresholdMaxBasalSpeed,
    thresholdMaxBolusDose = thresholdMaxBolusDose,
    checkSafety = checkSafety,
    checkNeedle = checkNeedle,
    needleFailedCount = needleFailedCount,
    isConnected = isConnected,
    needDiscard = needDiscard,
    isDiscard = isDiscard,
    isExtended = isExtended,
    isValid = isValid,
    isStopped = isStopped,
    stopMinutes = stopMinutes,
    stopMode = stopMode,
    isForceStopped = isForceStopped,
    runningMinutes = runningMinutes,
    infusedTotalBasalAmount = infusedTotalBasalAmount,
    infusedTotalBolusAmount = infusedTotalBolusAmount,
    pumpState = pumpState,
    mode = mode,
    bolusActionSeq = bolusActionSeq
)
