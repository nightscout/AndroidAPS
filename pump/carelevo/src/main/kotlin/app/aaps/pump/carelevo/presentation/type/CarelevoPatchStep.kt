package app.aaps.pump.carelevo.presentation.type

enum class CarelevoPatchStep {
    PROFILE_GATE,
    SELECT_INSULIN,
    PATCH_START,
    SET_AMOUNT,
    PATCH_CONNECT,
    SAFETY_CHECK,
    SITE_LOCATION,
    PATCH_ATTACH,
    NEEDLE_INSERTION
}
