<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-feature
        android:name="android.hardware.telephony"
        android:required="false" />

    <uses-permission android:name="android.permission.RECEIVE_SMS" />
    <uses-permission android:name="android.permission.RECEIVE_MMS" />
    <uses-permission android:name="android.permission.SEND_SMS" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="app.aaps.weardata.permission" />
    <!-- For DataLayerListenerServiceMobile below. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_CONNECTED_DEVICE" />

    <permission
        android:name="app.aaps.weardata.permission"
        android:label="AAPS WEAR DATA"
        android:protectionLevel="signature" />

    <application>

        <service
            android:name=".nsclientV3.services.NSClientV3Service"
            android:enabled="true"
            android:exported="false" />

        <!-- These two were declared from :plugins:main's manifest, which merging made work but which
             put them a module away from the classes they name. -->
        <activity
            android:name=".wear.activities.CwfInfosActivity"
            android:exported="false"
            android:theme="@style/AppTheme" />

        <service
            android:name=".wear.wearintegration.DataLayerListenerServiceMobile"
            android:foregroundServiceType="connectedDevice"
            android:exported="true">
            <intent-filter>
                <action android:name="com.google.android.gms.wearable.CHANNEL_EVENT" />
                <action android:name="com.google.android.gms.wearable.DATA_CHANGED" />
            </intent-filter>
            <intent-filter>
                <action android:name="com.google.android.gms.wearable.CAPABILITY_CHANGED" />
                <data
                    android:host="*"
                    android:scheme="wear" />
            </intent-filter>
            <intent-filter>
                <!-- listeners receive events that match the action and data filters -->
                <action android:name="com.google.android.gms.wearable.MESSAGE_RECEIVED" />

                <data
                    android:host="*"
                    android:pathPrefix="@string/path_rx_bridge"
                    android:scheme="wear" />
            </intent-filter>
            <intent-filter>
                <!-- listeners receive events that match the action and data filters -->
                <action android:name="com.google.android.gms.wearable.MESSAGE_RECEIVED" />

                <data
                    android:host="*"
                    android:pathPrefix="@string/path_rx_data_bridge"
                    android:scheme="wear" />
            </intent-filter>
        </service>

        <activity
            android:name=".openhumans.ui.OHLoginActivity"
            android:exported="true"
            android:label="Setup Open Humans"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data
                    android:host="setup-openhumans"
                    android:scheme="androidaps" />
            </intent-filter>
        </activity>

        <receiver
            android:name="app.aaps.plugins.sync.wear.receivers.WearDataReceiver"
            android:enabled="true"
            android:exported="true"
            android:permission="app.aaps.weardata.permission">
            <intent-filter>
                <!-- Receiver from aapsclient -->
                <action android:name="app.aaps.aapsclient.weardata" />
            </intent-filter>
        </receiver>

        <activity
            android:name="net.openid.appauth.RedirectUriReceiverActivity"
            android:exported="true"
            tools:node="replace">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <!--  Setting host bellow prevents callback because of missing /.well-known/assetlinks.json                  android:host=""-->
                <data
                    android:path="/callback/tidepool"
                    android:scheme="aaps"
                    tools:ignore="AppLinkUrlError" />
            </intent-filter>
        </activity>
        <activity
            android:name="app.aaps.plugins.sync.tidepool.auth.AuthFlowIn"
            android:exported="true" />

    </application>

</manifest>