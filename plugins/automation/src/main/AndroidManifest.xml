<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission
        android:name="android.permission.ACCESS_BACKGROUND_LOCATION"
        tools:ignore="BackgroundLocationPolicy" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <uses-permission
        android:name="android.permission.BLUETOOTH_SCAN"
        android:usesPermissionFlags="neverForLocation" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" />

    <application>

        <!-- The location trigger's service. Declared here, in the module that owns it. It sat in
             :app while Dagger wired it, because Dagger answers injection with generated Java and a
             multiplatform module has no Java compile step. Metro generates none, so it came home. -->
        <service
            android:name="app.aaps.plugins.automation.services.LocationService"
            android:exported="false"
            android:foregroundServiceType="location" />

        <!-- Rings reminders scheduled through ReminderScheduler. Same story as above. -->
        <receiver
            android:name="app.aaps.plugins.automation.TimerReminderReceiver"
            android:exported="false" />

    </application>

</manifest>