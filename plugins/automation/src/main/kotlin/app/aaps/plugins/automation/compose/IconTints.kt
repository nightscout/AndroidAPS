package app.aaps.plugins.automation.compose

import androidx.compose.ui.graphics.Color

/**
 * Shared semantic tint palette for Trigger/Action icons. Values match the
 * light-mode `AapsTheme.elementColors` and are applied as fixed Color (same
 * in light/dark) to stay readable against the M3 surface container on both.
 */
object IconTint {

    val Glucose = Color(0xFFE93057)
    val Insulin = Color(0xFF1E88E5)
    val Carbs = Color(0xFFE19701)
    val Delta = Color(0xFF66BB6A)
    val AutoSens = Color(0xFF008585)
    val Device = Color(0xFF78909C)
    val Profile = Color(0xFF66BB6A)
    val Exercise = Color(0xFF42A5F5)
    val TempTarget = Color(0xFF6BD16B)
    val Heart = Color(0xFFE93057)
    val Time = Color(0xFFFEAF05)
    val Location = Color(0xFFE93057)
    val Network = Color(0xFF42A5F5)
    val Alarm = Color(0xFFE93057)
    val Announce = Color(0xFFCF8BFE)
    val Sms = Color(0xFF42A5F5)
    val System = Color(0xFF78909C)
    val CarePortal = Color(0xFFFEAF05)
    val Smb = Color(0xFF66BB6A)
    val Autotune = Color(0xFFE19701)
    val Stop = Color(0xFFE93057)
}
