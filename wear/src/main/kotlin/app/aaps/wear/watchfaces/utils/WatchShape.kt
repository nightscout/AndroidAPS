package app.aaps.wear.watchfaces.utils

/**
 * Enumeration of possible watch screen shapes.
 */
enum class WatchShape {

    /**
     * This is normally a placeholder until the watch face is informed by the system what shape it is.
     */
    UNKNOWN,
    SQUARE,
    CIRCLE
}
